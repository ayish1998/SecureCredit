{
  "name": "Automated Test Runner",
  "description": "Automatically runs tests when TypeScript files are saved",
  "trigger": {
    "type": "file_save",
    "pattern": "**/*.{ts,tsx}"
  },
  "actions": [
    {
      "type": "shell_command",
      "command": "npm run test -- --watchAll=false --passWithNoTests",
      "description": "Run test suite for saved files"
    }
  ],
  "enabled": true,
  "settings": {
    "debounce": 2000,
    "parallel": false
  }
}
